# Database URL for PostgreSQL connection (required in production)
DATABASE_URL=

# Secret key for JWT token generation (use something secure in production)
SECRET_KEY=your-secure-key-here

# JWT algorithm (default is HS256)
ALGORITHM=HS256

# Access token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Port for the API server
PORT=8000

# Anthropic API key for AI event parsing (admin AI Import feature)
ANTHROPIC_API_KEY=
